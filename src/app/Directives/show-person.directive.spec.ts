import { ShowPersonDirective } from './show-person.directive';

describe('ShowPersonDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowPersonDirective();
    expect(directive).toBeTruthy();
  });
});
